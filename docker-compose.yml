services:
  denon-remote:
    build: .
    container_name: denon-ceol-remote
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - PORT=3000
      - SCAN_SUBNET=192.168.1
